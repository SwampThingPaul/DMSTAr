% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Case_Sim.R
\name{dmsta_make_cell}
\alias{dmsta_make_cell}
\title{Create a DMSTA network cell definition}
\usage{
dmsta_make_cell(
  label,
  params,
  ttankS,
  DownCell = 0L,
  Qin_Frac = 0,
  RecycleIndex = NULL,
  SplitterFrac = NULL
)
}
\arguments{
\item{label}{Character scalar. Cell label (use \code{"SPLITTER"} for the
splitter cell if your case uses one).}

\item{params}{Named list of per-cell parameters (must include at least
\code{A_cell}, \code{DutyCycle}, \code{Zinit}, \code{C_init_ppb}, \code{Y_init_mgm2},
plus fields required by the selected kinetics builders).}

\item{ttankS}{Numeric scalar > 0. Effective number of tanks (may be fractional).}

\item{DownCell}{Integer index of the downstream cell (0 means terminal/out of system).}

\item{Qin_Frac}{Numeric scalar. Fraction of watershed inflow assigned to this cell
in network routing (case runner may override internally for “already allocated” inflows).}

\item{RecycleIndex}{Integer index of the cell receiving recycled seepage flow/mass.
\code{0} or \code{NA} indicates default handling (often self).}

\item{SplitterFrac}{Optional. Splitter routing fractions (only used if
\code{label} is \code{"SPLITTER"}). May be a numeric vector (length = number of cells)
or a named vector/list mapping downstream indices to fractions.}
}
\value{
A named list representing a cell definition, suitable for inclusion
in the \code{cells} argument of \code{\link[=dmsta_flowP_case]{dmsta_flowP_case()}}.
}
\description{
Constructs a cell definition used by \code{\link[=dmsta_flowP_case]{dmsta_flowP_case()}} network/case
simulations. The function attaches:
\itemize{
\item per-cell tank specification \code{ttankS},
\item routing fields (\code{DownCell}, \code{Qin_Frac}, \code{RecycleIndex}),
\item optional splitter specification (\code{SplitterFrac}),
\item per-cell kinetics parameters (\code{ppar}) using built-in modules
(\code{STA}, \code{PSTA}, \code{RES}),
\item per-cell constants list used by the phosphorus derivative.
}
}
\seealso{
\code{\link[=dmsta_flowP_case]{dmsta_flowP_case()}} for running a multi-cell simulation.
}
