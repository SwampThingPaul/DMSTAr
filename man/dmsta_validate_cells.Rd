% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Network_routing.R
\name{dmsta_validate_cells}
\alias{dmsta_validate_cells}
\title{Validate and normalize network cell definitions (internal)}
\usage{
dmsta_validate_cells(cells)
}
\arguments{
\item{cells}{A list of cell definitions (as created by \code{\link[=dmsta_make_cell]{dmsta_make_cell()}}
or assembled manually).}
}
\value{
The normalized \code{cells} list (with defaults filled in). Invisibly
returns \code{cells} but typically used as \code{cells <- dmsta_validate_cells(cells)}.
}
\description{
Checks that \code{cells} is a non-empty list of cell definitions and that each
cell contains the minimum required fields for a coupled hydrology + phosphorus
simulation.
}
\details{
The validator enforces:
\itemize{
\item required structural fields: \code{params}, \code{ttankS}, \code{ppar}, \code{constants}
\item required \code{ppar} fields used by the phosphorus derivative
\item required \code{constants} fields used by the phosphorus derivative
\item DMSTA convention for recycle indices (0 means self)
\item at most one splitter cell labeled \code{"SPLITTER"} (case-insensitive)
}

The function also fills defaults for some routing fields if missing.
}
