% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpwem.R
\name{lpwem-internal}
\alias{lpwem-internal}
\alias{lpwem_areal_to_kgd}
\alias{lpwem_build_tis}
\alias{lpwem_rstar_vec}
\alias{lpwem_J_vec}
\alias{lpwem_Q_along_tanks}
\alias{lpwem_tp_euler_step}
\title{LPWEM internal helper functions}
\usage{
lpwem_areal_to_kgd(rate_mgm2d, A_km2)

lpwem_build_tis(A_cell_km2, N = 6L)

lpwem_rstar_vec(N, r1_mgm2d, rN_mgm2d)

lpwem_J_vec(TP_ppb, Q_hm3d, W_m, k0, alpha)

lpwem_Q_along_tanks(Q0_hm3d, QN_hm3d, N)

lpwem_tp_euler_step(TP, Vavg_hm3, Q0_hm3d, QN_hm3d, tis, pars, dt_day)
}
\arguments{
\item{rate_mgm2d}{Numeric areal rate in \eqn{mg m^{-2} d^{-1}}.}

\item{A_km2}{Numeric area in \eqn{km^2}.}

\item{A_cell_km2}{Total wetland surface area in \eqn{km^2}.}

\item{N}{Integer number of tanks.}

\item{r1_mgm2d}{Return flux at the first tank (\eqn{mg m^{-2} d^{-1}}).}

\item{rN_mgm2d}{Return flux at the last tank (\eqn{mg m^{-2} d^{-1}}).}

\item{TP_ppb}{Numeric vector of tank TP concentrations (ppb).}

\item{Q_hm3d}{Numeric vector of tank flows (\eqn{hm^3 d^{-1}}), typically the
outflow from each tank.}

\item{W_m}{Numeric wetland width parameter (\eqn{m}).}

\item{k0}{Numeric uptake coefficient \eqn{k_0}.}

\item{alpha}{Numeric coefficient \eqn{\alpha} applied to \eqn{Q/W}.}

\item{Q0_hm3d}{Wetland inflow rate (\eqn{hm^3 d^{-1}}).}

\item{QN_hm3d}{Wetland outflow rate (\eqn{hm^3 d^{-1}}).}

\item{TP}{Numeric vector of length \code{tis$N} of tank TP concentrations (ppb).}

\item{Vavg_hm3}{Step-average wetland volume (\eqn{hm^3}).}

\item{tis}{Tanks-in-series structure as returned by \code{\link[=lpwem_build_tis]{lpwem_build_tis()}}.}

\item{pars}{List of LPWEM parameters; must include \code{Cin_ppb}, \code{W_m},
\code{k0}, \code{alpha}, \code{r1_mgm2d}, \code{rN_mgm2d}.}

\item{dt_day}{Micro-step duration in days.}
}
\value{
Numeric mass rate in \eqn{kg d^{-1}}.

A list with:
\itemize{
\item \code{N}: integer number of tanks
\item \code{A_i}: numeric vector of length \code{N} of tank areas (\eqn{km^2})
}

Numeric vector of length \code{N} with the return flux for each tank.

Numeric vector of uptake flux \eqn{J_i} in \eqn{mg m^{-2} d^{-1}}.

Numeric vector of length \code{N + 1} giving \verb{Q_0, ..., Q_N}
in \eqn{hm^3 d^{-1}}.

A list with:
\itemize{
\item \code{TP_new}: updated TP vector (ppb)
\item \code{diag}: diagnostics including \code{h_m}, \code{Q_along},
\code{J_mgm2d}, \code{r_mgm2d}, and per-tank mass-balance terms
}
}
\description{
Internal building blocks used by the LPWEM (Lake Phosphorus Wetland
Ecosystem Model) implementation for transport and kinetic updates across
a tanks-in-series wetland representation.

Converts an areal flux expressed in \eqn{mg m^{-2} d^{-1}} to a mass rate
in \eqn{kg d^{-1}} by multiplying by area in \eqn{km^2}.

Constructs an equal-area tanks-in-series (TIS) representation for a wetland
cell, where each tank has area \eqn{A_i = A_{cell}/N}.

Computes the LPWEM return flux \eqn{r^*_i} (\eqn{mg m^{-2} d^{-1}}) across a
tanks-in-series chain, linearly interpolated from the first tank value
\code{r1_mgm2d} to the last tank value \code{rN_mgm2d}.

Computes the LPWEM areal uptake flux \eqn{J_i} (\eqn{mg m^{-2} d^{-1}}) from
tank concentrations and hydrologic forcing using:
\deqn{J = k_0 \left(1 + \alpha \frac{Q}{W}\right) TP^2}
where \eqn{Q} is in \eqn{m^3 d^{-1}}, \eqn{W} is in \eqn{m}, and \eqn{TP} is
in \eqn{ppb} (treated as \eqn{mg m^{-3}}). Unit consistency is carried by
\eqn{k_0}.

Constructs the sequence \eqn{Q_i} for \eqn{i = 0..N} (length \eqn{N+1}) by
linear interpolation between wetland inflow \eqn{Q_0} and wetland outflow
\eqn{Q_N}.

Advances the tank TP concentration state one micro time step using an explicit
Euler update of the mass-balance formulation across a tanks-in-series chain.
}
\details{
These functions are not intended to be called directly by end users.

The conversion is implicit because:
\deqn{(mg/m^2/day) * (km^2) = (mg/m^2/day) * (10^6 m^2) = 10^6 mg/day = 1 kg/day.}

The implementation updates per-tank mass \eqn{M_i} and converts back to
concentration. Water depth is approximated as:
\deqn{h = V/A}
with \eqn{V} in \eqn{hm^3} and \eqn{A} in \eqn{km^2}, yielding \eqn{h} in
meters because \eqn{1\,m * 1\,km^2 = 1\,hm^3}.

Flow is interpolated along tanks from \eqn{Q_0} to \eqn{Q_N}, and the return
flux \eqn{r^*} is interpolated linearly from the first to last tank values.
}
\examples{
\dontrun{
lpwem_build_tis(12, N = 6)
}
\dontrun{
lpwem_rstar_vec(N = 4, r1_mgm2d = 10, rN_mgm2d = 2)
}
\dontrun{
lpwem_Q_along_tanks(Q0_hm3d = 2, QN_hm3d = 1, N = 4)
}

}
\keyword{internal}
