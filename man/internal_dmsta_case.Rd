% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Case_Sim.R
\name{internal_dmsta_case}
\alias{internal_dmsta_case}
\alias{dmsta_daily_avg_volume}
\alias{dmsta_prepare_cells_modules}
\alias{dmsta_case_components}
\title{DMSTA network/case helpers (internal)}
\usage{
dmsta_daily_avg_volume(step_list)

dmsta_prepare_cells_modules(cells)

dmsta_case_components(
  df,
  keep_Q17 = TRUE,
  keep_P = TRUE,
  P_prefix = "^P_",
  keep_list_cols = c("mass_budget"),
  keep_extra = TRUE
)
}
\arguments{
\item{step_list}{A list of substep records. Each element should be a list
containing numeric fields \code{Vo} (start volume), \code{V} (end volume),
and \code{Dt} (substep duration in days).}

\item{cells}{A list of cell definitions. Each element must contain
\verb{$params} with the raw parameters needed by the kinetics builder
(and optional constants inputs).}

\item{df}{A data.frame containing DMSTA-style result columns.}

\item{keep_Q17}{Logical; if \code{TRUE}, keep seep recycle fields (Q17/L17/C17) if present.}

\item{keep_P}{Logical; if \code{TRUE}, carry through phosphorus budget fields.}

\item{P_prefix}{Regular expression used to match P-budget fields (default \code{"^P_"}).}

\item{keep_list_cols}{Character vector of list-column names to carry through if present.}

\item{keep_extra}{Logical; if \code{TRUE}, carry through any remaining fields not
in the standardized output.}
}
\value{
Numeric scalar. Daily-average volume integral (same volume units as
\code{Vo} and \code{V}).

The updated \code{cells} list with \verb{$ppar} and \verb{$constants}
added to each element.

A data.frame with standardized columns, plus optional carry-through fields.
}
\description{
Internal helpers used for DMSTA multi-cell / network (case) simulations,
including per-cell initialization, validation, per-cell kinetics preparation,
daily-mean volume calculations, and output column normalization.

Computes the DMSTA-style daily average volume as the time integral of the
substep mean volume:
\deqn{V_{day} = \sum_k \left(\frac{V_{o,k} + V_{k}}{2}\right)\Delta t_k}
where each substep record provides \code{Vo}, \code{V}, and \code{Dt}.

Populates each cell definition with:
\itemize{
\item \code{ppar}: per-cell phosphorus kinetic parameters (typically from
\code{build_P_kin_slots(mods = c("STA","PSTA","RES"), ...)}),
\item \code{constants}: a per-cell constants list used by the phosphorus
mass-balance derivative (e.g., concentration caps, deposition terms,
seepage bookkeeping fractions).
}

Utility to standardize output column names and structure between per-cell and
case-level result data frames. The function:
\itemize{
\item maps known aliases (e.g., \code{RainVol_total} -> \code{RainVol}),
\item builds a consistent set of inflow/outflow component columns,
\item optionally keeps seep recycle (Q17/L17/C17),
\item optionally carries through P-budget fields (e.g., columns prefixed with \code{P_}),
\item optionally preserves specified list-columns (e.g., \code{mass_budget}).
}
}
\details{
These functions are used by \code{dmsta_flowP_case()} and related
network orchestration logic.

The function returns the updated \code{cells} object.

This function uses the phosphorus model registry exposed as
\code{P_MODEL_BUILDERS} and assumes the \code{STA}, \code{PSTA}, and \code{RES}
builders are available in the registry.
}
\seealso{
\code{dmsta_validate_cells()} for strict validation of the
resulting cell definitions.
}
\keyword{internal}
