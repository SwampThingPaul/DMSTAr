% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/P_kinetics.R
\name{build_P_kin_slots}
\alias{build_P_kin_slots}
\title{Build multi-slot phosphorus kinetics parameter vectors}
\usage{
build_P_kin_slots(
  mods,
  registry = NULL,
  pparams,
  Dpy = 365.25,
  DutyCycle = 0.95,
  derive_PModel = TRUE,
  default_PModel = 1L,
  ...
)
}
\arguments{
\item{mods}{Character vector of model identifiers (e.g., \code{c("STA","RES")}).}

\item{registry}{Optional model registry (named list of builder functions).
If \code{NULL} (default), the package's default registry is used.}

\item{pparams}{Named \code{list} of raw parameters. The same \code{pparams} is
passed to every slot.}

\item{Dpy}{Numeric scalar. Time steps per year (default \code{365.25}).}

\item{DutyCycle}{Numeric scalar in \eqn{[0,1]}. Duty cycle multiplier.}

\item{derive_PModel}{Logical; reserved for future use.}

\item{default_PModel}{Integer scalar; reserved for future use.}

\item{...}{Additional arguments passed to \code{\link[=build_P_kinetics]{build_P_kinetics()}}.}
}
\value{
A named \code{list} containing:
\describe{
\item{K1, K2, K3}{Numeric vectors of length \code{K}.}
\item{Chalf, Z_1, Z_2, Z_3, K2Coef}{Numeric vectors of length \code{K}.}
\item{Kslots}{Integer scalar giving \code{K}.}
\item{SeasonalFactor, Ytrans, Ysigma, Czero, PModel}{Global scalars.}
\item{mods}{Character vector of model identifiers.}
}
}
\description{
Builds phosphorus kinetics for multiple model "slots" and assembles the
results into K-length vectors (where \code{K = length(mods)}).
}
\details{
Each slot is constructed by calling \code{\link[=build_P_kinetics]{build_P_kinetics()}} with the same
parameter list \code{pparams}. This is useful when a simulation uses multiple
phosphorus modules that differ by model type but share the same raw parameter set.
}
\examples{
\donttest{
pparams <- list(
  # shared / STA
  C1000 = 1000, Cstar = 100, Ks_per_yr = 1,
  Z1 = 10, Z2 = 30, Z3 = 60,
  K2Coef1 = 0.1, Chalf = 50, SeasonalFactor = 1,
  # PSTA
  Ytrans = 1, Ysigma = 1, C1000_2 = 1000, ks_2 = 1, zh_2 = 10,
  # RES
  k_depth_penalty = 0.5
)

out <- build_P_kin_slots(
  mods = c("STA", "PSTA", "RES"),
  pparams = pparams,
  Dpy = 365.25,
  DutyCycle = 0.95
)
out$K1
out$Z_1
}

}
