% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/P_kinetics.R
\name{build_P_kinetics}
\alias{build_P_kinetics}
\title{Build phosphorus kinetics parameters for a registered model}
\usage{
build_P_kinetics(mod_type, Dpy = 365.25, DutyCycle = NULL, pparams, ...)
}
\arguments{
\item{mod_type}{Character scalar. Model identifier (e.g., \code{"STA"}).}

\item{Dpy}{Numeric scalar. Time steps per year (default \code{365.25}).}

\item{DutyCycle}{Numeric scalar in \eqn{[0, 1]}. Duty-cycle multiplier applied
to time-scaled rate parameters.}

\item{pparams}{Named \code{list}. Raw parameters expected by the chosen model
builder.}

\item{...}{Additional arguments passed through to the underlying model builder.}
}
\value{
A named \code{list} of standardized parameters with elements
\code{K1}, \code{K2}, \code{K3}, and \code{PModel}. The result has class
\code{"P_kinetics"}.
}
\description{
Builds a standardized parameter list for a selected phosphorus model type
(e.g., \code{"STA"}, \code{"PSTA"}, \code{"RES"}) using a registered
model-builder function, then derives kinetic coefficients \code{K1}, \code{K2},
\code{K3} and the kinetic model indicator \code{PModel} via
\code{compute_DMSTA_kvals()}.
}
\details{
Model builders are obtained from the package phosphorus model registry.

The selected model builder should return a list containing at minimum
\code{C1000}, \code{Cstar}, and \code{Ks} (on the target time step).
Additional fields returned by the builder (e.g., \code{Z1}, \code{Z2}, \code{Chalf})
are preserved. The function appends \code{K1}, \code{K2}, \code{K3}, and \code{PModel}.

The result is assigned class \code{"P_kinetics"} (prepended to any existing classes).
}
\examples{
# Example assumes DMSTAr ships with a registered "STA" builder.
pparams <- list(
  C1000 = 1, Cstar = 0.2, Ks_per_yr = 0.5,
  Z1 = 10, Z2 = 30, Z3 = 60,
  K2Coef1 = 0.1, Chalf = 0.2, SeasonalFactor = 1,
  DutyCycle = 0.95
)
pars <- build_P_kinetics("STA", Dpy = 365.25, pparams = pparams)
pars$K1
pars$PModel

}
