% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/P_engine.R
\name{internal_dmsta_phosphorus}
\alias{internal_dmsta_phosphorus}
\alias{dmsta_deriv_mass}
\alias{dmsta_rk4_P_step}
\alias{dmsta_flowP_day}
\title{DMSTA phosphorus engine (internal)}
\usage{
dmsta_deriv_mass(state, drivers, ppar, constants)

dmsta_rk4_P_step(
  M,
  S,
  args_base,
  Dt,
  ppar,
  constants,
  clamp = TRUE,
  Mmin = 0,
  Smin = 0
)

dmsta_flowP_day(
  V,
  P_state,
  tanks,
  inputs,
  params,
  ppar,
  constants,
  Nsteps = 4L,
  Z_plant = 0
)
}
\arguments{
\item{state}{Named list containing tank state:
\describe{
\item{M}{Numeric scalar. Water-column mass/state for the tank (kg).}
\item{S}{Numeric scalar. Sediment/areal store for the tank (kg).}
}}

\item{drivers}{Named list of stage-specific drivers and geometry. Expected fields include:
\describe{
\item{A_tank}{Tank area (same area units as \code{A_cell}).}
\item{A_cell}{Cell area.}
\item{V_tank_avg}{Average tank volume for the substep (e.g., hm^3).}
\item{Vdel}{Change in tank volume over the substep (same units as \code{V_tank_avg}).}
\item{StepFrac}{RK stage fraction in \{0, 0.5, 1\}.}
\item{Ddt}{Effective duration used in limiter logic (days).}
\item{Qo_tank}{Tank outflow rate (volume/day).}
\item{Li_tank}{Tank inflow load rate (kg/day).}
\item{Rain}{Rain rate (m/day).}
\item{Seepout}{Cell seep-out flow rate (volume/day).}
\item{Seepin}{Cell seep-in flow rate (volume/day).}
\item{Z_plant}{Rolling/representative plant depth used for reservoir blending (m).}
\item{julian}{Julian day (numeric) used for seasonal modulation.}
\item{Mo_fix}{Reference M for the RK stage limiter (kg).}
\item{So_fix}{Reference S for the RK stage limiter (kg).}
}}

\item{ppar}{Phosphorus kinetic parameter list, typically from \code{build_P_kin_slots()}.}

\item{constants}{Constants list used by \code{dmsta_deriv_mass()}.}

\item{M}{Numeric scalar. Current water-column state for the tank (kg).}

\item{S}{Numeric scalar. Current areal/sediment store for the tank (kg).}

\item{args_base}{Named list with element \code{drivers}, itself a named list
containing all fields required by \code{dmsta_deriv_mass()} except
\code{StepFrac}, \code{Ddt}, \code{Mo_fix}, \code{So_fix}.}

\item{Dt}{Numeric scalar. Step duration (days), typically \code{1/Nsteps}.}

\item{clamp}{Logical; if \code{TRUE}, clamp outputs to \code{Mmin} and \code{Smin}.}

\item{Mmin}{Numeric scalar. Minimum allowed \code{M} after clamping.}

\item{Smin}{Numeric scalar. Minimum allowed \code{S} after clamping.}

\item{V}{Numeric scalar. Starting cell volume (e.g., hm^3).}

\item{P_state}{Named list with \code{M} and \code{S} vectors (kg), each of
length \code{tanks$Ntanks}.}

\item{tanks}{Tank geometry list as returned by \code{dmsta_build_tanks()},
including \code{Ntanks}, \code{A_Tank}, \code{F_Tank}, and \code{Fcum}.}

\item{inputs}{Named list of day forcings and controls. Must include at least
\code{Date}, \code{Qi}, \code{Ci}, \code{Rain}, \code{Et}, and \code{Zcontrol}
(plus optional releases and recycle fields used downstream).}

\item{params}{Hydrology parameter list passed to \code{dmsta_flow_day_steps()}.
Must include \code{A_cell}.}

\item{Nsteps}{Integer. Number of RK substeps per day.}

\item{Z_plant}{Numeric scalar. Rolling/representative plant depth (m) used for blending.}
}
\value{
A named list with elements:
\describe{
\item{dMdt}{Numeric scalar. Derivative of \code{M} (kg/day).}
\item{dSdt}{Numeric scalar. Derivative of \code{S} (kg/day).}
\item{flux}{List of per-area flux terms (rates) used internally:
\code{P_uptake}, \code{P_recycle}, \code{P_sed}, \code{P_direct}.}
\item{diag}{List of diagnostics: \code{C}, \code{Y}, \code{z}.}
}

A named list containing updated states, mean derivatives, and
averaged diagnostics over the step:
\describe{
\item{M_new, S_new}{Updated tank states (kg).}
\item{dMdt_ts, dSdt_ts}{Step-mean derivatives (kg/day).}
\item{P_uptake_ts, P_recycle_ts, P_sed_ts, P_direct_ts}{Step-mean flux terms (rates).}
\item{C_ts, Y_ts, z_ts}{Step-mean diagnostics.}
}

A named list with components:
\describe{
\item{results}{Daily totals and end-of-day state, including \code{P_state_end}.}
\item{budgets}{Lists of \code{water} and \code{mass} budgets (mass may be \code{NULL}).}
\item{meta}{Metadata and (by default) hydrology substep details for debugging/coupling.}
}
}
\description{
Internal helpers implementing DMSTA coupled phosphorus mass balance within
a tank series, including derivative evaluation and RK4 stepping.

Computes instantaneous derivatives \code{dMdt} and \code{dSdt} for one tank at
a single RK stage. This corresponds to DMSTA "DerivMass" logic and includes
uptake, recycle, sediment/burial, direct sedimentation, and advective transport
driven by tank outflow and seepage.

Advances tank states \code{M} and \code{S} forward by one substep of length
\code{Dt} (days) using a 4th-order Runge-Kutta integrator. Stage-specific
driver fields \code{StepFrac}, \code{Ddt}, \code{Mo_fix}, and \code{So_fix}
are injected before calling \code{dmsta_deriv_mass()}.

Integrates DMSTA hydrology over one day (via \code{dmsta_flow_day_steps()})
and then integrates phosphorus mass balance across tanks using RK4 substeps.
Returns daily totals for flows and loads, updated tank states, and optional
water and mass budget diagnostics.
}
\details{
These functions are used by \code{\link[=dmsta_flowP_series]{dmsta_flowP_series()}}.

Internal diagnostics are computed from stage volume \code{vV}:
\itemize{
\item \code{C = M / vV} (ppb-like concentration)
\item \code{Y = S / A_tank} (areal store, mg/m^2 equivalent)
\item \code{z = vV / A_tank} (m)
}

Kinetics differ by \code{ppar$PModel}:
\itemize{
\item \code{PModel == 2L}: transformed kinetics using slot-1 coefficients.
\item otherwise: blended "transition" kinetics combining slots 1--2 and
optionally slot-3 reservoir blending using \code{Z_plant}.
}
}
\keyword{internal}
