% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmsta_P_engine.R
\name{internal_dmsta_phosphorus}
\alias{internal_dmsta_phosphorus}
\alias{dmsta_DerivMass}
\alias{dmsta_rk4_P_step}
\alias{dmsta_euler_P_step}
\alias{dmsta_P_step}
\title{DMSTA phosphorus engine (internal)}
\usage{
dmsta_DerivMass(state, drivers, ppar, constants)

dmsta_rk4_P_step(
  M,
  S,
  args_base,
  Dt,
  ppar,
  constants,
  clamp = TRUE,
  Mmin = 0,
  Smin = 0
)

dmsta_euler_P_step(
  M,
  S,
  args_base,
  Dt,
  ppar,
  constants,
  clamp = TRUE,
  Mmin = 0,
  Smin = 0
)

dmsta_P_step(
  method = c("RK4", "Euler"),
  M,
  S,
  args_base,
  Dt,
  ppar,
  constants,
  ...
)
}
\arguments{
\item{state}{Named list containing tank state:
\describe{
\item{M}{Numeric scalar. Water-column mass/state for the tank (kg).}
\item{S}{Numeric scalar. Sediment/areal store for the tank (kg).}
}}

\item{drivers}{Named list of stage-specific drivers and geometry. Expected fields include:
\describe{
\item{A_tank}{Tank area (same area units as \code{A_cell}).}
\item{A_cell}{Cell area.}
\item{V_tank_avg}{Average tank volume for the substep (e.g., hm^3).}
\item{Vdel}{Change in tank volume over the substep (same units as \code{V_tank_avg}).}
\item{StepFrac}{RK stage fraction in \{0, 0.5, 1\}.}
\item{Ddt}{Effective duration used in limiter logic (days).}
\item{Qo_tank}{Tank outflow rate (volume/day).}
\item{Li_tank}{Tank inflow load rate (kg/day).}
\item{Rain}{Rain rate (m/day).}
\item{Seepout}{Cell seep-out flow rate (volume/day).}
\item{Seepin}{Cell seep-in flow rate (volume/day).}
\item{Z_plant}{Rolling/representative plant depth used for reservoir blending (m).}
\item{julian}{Julian day (numeric) used for seasonal modulation.}
\item{Mo_fix}{Reference M for the RK stage limiter (kg).}
\item{So_fix}{Reference S for the RK stage limiter (kg).}
}}

\item{ppar}{List of phosphorus model parameters (output of
\code{build_P_kin_slots()}).}

\item{constants}{List of physical and chemical constants used by
\code{dmsta_DerivMass()}.}

\item{M}{Numeric scalar. Current water-column phosphorus mass (kg).}

\item{S}{Numeric scalar. Current labile (biota/soil) phosphorus mass (kg).}

\item{args_base}{List containing at least \verb{$drivers}, a list of
hydrologic and geometric drivers required by \code{dmsta_DerivMass()}.}

\item{Dt}{Numeric scalar. Sub-step duration (days).}

\item{clamp}{Logical. If \code{TRUE}, negative or non-finite updated
masses are clamped to \code{Mmin} and \code{Smin}. Default is \code{TRUE}.}

\item{Mmin}{Numeric scalar. Minimum allowable water-column P mass (kg).
Default is 0.}

\item{Smin}{Numeric scalar. Minimum allowable labile P mass (kg).
Default is 0.}

\item{method}{Character string specifying the P integrator to use.
One of \code{"RK4"} or \code{"Euler"}.}

\item{...}{Additional arguments passed through to the selected
integrator (e.g., clamping options).}
}
\value{
A named list with elements:
\describe{
\item{dMdt}{Numeric scalar. Derivative of \code{M} (kg/day).}
\item{dSdt}{Numeric scalar. Derivative of \code{S} (kg/day).}
\item{flux}{List of per-area flux terms (rates) used internally:
\code{P_uptake}, \code{P_recycle}, \code{P_sed}, \code{P_direct}.}
\item{diag}{List of diagnostics: \code{C}, \code{Y}, \code{z}.}
}

A named list containing updated states, mean derivatives, and
averaged diagnostics over the step:
\describe{
\item{M_new, S_new}{Updated tank states (kg).}
\item{dMdt_ts, dSdt_ts}{Step-mean derivatives (kg/day).}
\item{P_uptake_ts, P_recycle_ts, P_sed_ts, P_direct_ts}{Step-mean flux terms (rates).}
\item{C_ts, Y_ts, z_ts}{Step-mean diagnostics.}
}

A list with elements:
\describe{
\item{M_new}{Updated water-column phosphorus mass (kg).}
\item{S_new}{Updated labile phosphorus mass (kg).}
\item{dMdt_ts}{Instantaneous rate of change of \code{M} (kg/day).}
\item{dSdt_ts}{Instantaneous rate of change of \code{S} (kg/day).}
\item{flux}{Named list of phosphorus flux rates (per area),
including uptake, recycle, sedimentation, burial, and release.}
\item{diag}{Named list of diagnostic variables (e.g., concentration,
storage density, depth) evaluated at the derivative call.}
}

A list returned by the selected P integrator, containing
updated phosphorus state variables, instantaneous rates, fluxes,
and diagnostic values.
}
\description{
Internal helpers implementing DMSTA coupled phosphorus mass balance within
a tank series, including derivative evaluation and RK4 stepping.

Computes instantaneous derivatives \code{dMdt} and \code{dSdt} for one tank at
a single RK stage. This corresponds to DMSTA "DerivMass" logic and includes
uptake, recycle, sediment/burial, direct sedimentation, and advective transport
driven by tank outflow and seepage.

Advances tank states \code{M} and \code{S} forward by one substep of length
\code{Dt} (days) using a 4th-order Runge-Kutta integrator. Stage-specific
driver fields \code{StepFrac}, \code{Ddt}, \code{Mo_fix}, and \code{So_fix}
are injected before calling \code{dmsta_deriv_mass()}.

Performs a single Euler (forward) integration step for DMSTA phosphorus
mass balances over a sub-day interval. This integrator evaluates
\code{dmsta_DerivMass()} once at mid-step (\code{StepFrac = 0.5}),
matching DMSTA conventions for within-day process evaluation.
}
\details{
These functions are used by \code{\link[=dmsta_flowP_series]{dmsta_flowP_series()}}.

Internal diagnostics are computed from stage volume \code{vV}:
\itemize{
\item \code{C = M / vV} (ppb-like concentration)
\item \code{Y = S / A_tank} (areal store, mg/m^2 equivalent)
\item \code{z = vV / A_tank} (m)
}

Kinetics differ by \code{ppar$PModel}:
\itemize{
\item \code{PModel == 2L}: transformed kinetics using slot-1 coefficients.
\item otherwise: blended "transition" kinetics combining slots 1--2 and
optionally slot-3 reservoir blending using \code{Z_plant}.
}

The Euler P integrator is intended primarily for diagnostic,
debugging, and unit-testing purposes. For production runs and
DMSTA parity comparisons, \code{dmsta_rk4_P_step()} should be used.

This function does not implement any numerical integration logic
itself. It simply dispatches to the appropriate integrator based on
\code{method}. For strict DMSTA parity and production simulations,
\code{method = "RK4"} is recommended.

Dispatches phosphorus mass integration to the requested numerical
integrator. This function provides a unified interface for multiple
P integrators, mirroring the design of the DMSTAr hydrology engine.

Currently supported methods are:
\itemize{
\item \code{"RK4"}: Fourth-order Runge--Kutta integrator
(default, DMSTA parity).
\item \code{"Euler"}: Single-step Euler integrator
(diagnostic/debugging use).
}
}
\keyword{internal}
