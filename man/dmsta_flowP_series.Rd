% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/P_engine.R
\name{dmsta_flowP_series}
\alias{dmsta_flowP_series}
\title{Run coupled DMSTA hydrology and phosphorus over a time series}
\usage{
dmsta_flowP_series(
  series,
  params,
  pparams = NULL,
  ttankS = 3,
  Nsteps = 4L,
  N_plant = 30L,
  ppar = NULL,
  constants = NULL,
  tanks = NULL,
  V_init = NULL,
  init_P_state = NULL,
  return_steps = FALSE
)
}
\arguments{
\item{series}{A \code{data.frame} containing (at minimum) columns:
\describe{
\item{Date}{Date column coercible via \code{as.Date()}.}
\item{Qi}{Daily inflow rate (volume/day).}
\item{Ci}{Inflow concentration (ppb).}
\item{Rain}{Rain rate (m/day).}
\item{Et}{ET rate (m/day).}
\item{Zcontrol}{Control depth (m).}
}
Optional columns include \code{Qr0}, \code{Qr1}, \code{Qr2} (release components),
\code{RecycleQ} (recycle flow), and \code{RecycleM} (recycle mass load).}

\item{params}{Named list of hydrology + phosphorus parameters. Must include at least:
\describe{
\item{A_cell}{Cell area.}
\item{DutyCycle}{Duty-cycle multiplier used for kinetics building (if \code{ppar} not supplied).}
\item{Zinit}{Initial depth (cm) used to derive \code{V_init} if not provided.}
\item{C_init_ppb}{Initial water concentration (ppb) for state initialization.}
\item{Y_init_mgm2}{Initial areal store (mg/m^2) for state initialization.}
}
Additional fields are used if \code{ppar} and/or \code{constants} are not supplied
(e.g., deposition, seepage concentrations, and kinetic builder inputs).}

\item{pparams}{Optional named list of phosphorus parameters to merge into \code{params}
(via \code{modifyList()}); useful if P parameters are maintained separately.}

\item{ttankS}{Numeric scalar > 0. Effective number of tanks; may be fractional.}

\item{Nsteps}{Integer. RK substeps per day (default \code{4L}).}

\item{N_plant}{Integer. Window length (days) for rolling mean depth \code{Z_plant}.}

\item{ppar}{Optional precomputed kinetics parameter object from \code{build_P_kin_slots()}.
If \code{NULL}, it is built internally for modules \code{STA}, \code{PSTA}, and \code{RES}.}

\item{constants}{Optional constants list used by \code{dmsta_deriv_mass()}.
If \code{NULL}, defaults are constructed from \code{params}.}

\item{tanks}{Optional tank geometry from \code{dmsta_build_tanks()}. If \code{NULL}, built from
\code{params$A_cell} and \code{ttankS}.}

\item{V_init}{Optional initial volume (same units as hydrology volume). If \code{NULL},
derived from \code{params$Zinit} and \code{params$A_cell}.}

\item{init_P_state}{Optional initial phosphorus state list \code{list(M, S)} with vectors
of length \code{tanks$Ntanks}. If \code{NULL}, constructed from \code{params} via
\code{dmsta_p_init_state()}.}

\item{return_steps}{Logical; if \code{TRUE}, store per-day hydrology substep details in
\code{meta$steps}. This can be large.}
}
\value{
An object of class \code{"dmsta_result"} (a list) with elements:
\describe{
\item{results}{A data.frame of daily volumes, flows, concentrations, and loads.}
\item{budgets}{A list with \code{water} and \code{mass} budget data.frames.}
\item{meta}{Run metadata, including final states and optional per-day step details.}
}
}
\description{
Runs a coupled DMSTA simulation over multiple days, integrating hydrology and
phosphorus mass balance through a series of conceptual tanks. Hydrology is
computed with sub-daily RK steps and phosphorus is integrated over the same
substeps for each tank.
}
\details{
The function can build tank geometry, phosphorus kinetics, constants, and
initial states automatically from \code{params}, or accept precomputed objects.

Internally, control-depth neighbor values are obtained using \code{neighbors_zcontrol()}.
The function also uses \code{build_P_kin_slots()} and \code{validate_P_paramsK()} when
\code{ppar} is not supplied.
}
\examples{
\dontrun{
# Read data (internal)
data(series)
series <- series[1:370,]; # for example, limit input file

# Data formatting
series$Qi <- cfs_to_hm3d(series$Flow) # cfs to hm3/d
series$Rain <- in_to_m(series$Rainfall) # inches to meters per day
series$Et <- in_to_m(series$ET)
series$Zcontrol <- 0/100 # meters; setting to zero to see what happens
# If you have release series; otherwise set to 0
series$Qr0 <- 0   # constrained outflow (forced Q) if used
series$Qr1 <- 0   # release 1
series$Qr2 <- 0   # release 2
series$Ci <- series$Conc

# input parameters
params <- list(
 A_cell = 2.19, # km2
 Zmin   = 2,              # cm
 Vmin   = 0,              # hm3
 Q_a = 1.0, # qcoef_a; discharge coef
 Q_b = 4.0, # qcoef_b; discharge exponent
 Zweir = 0, # cm; qcoef_offset; depth offset for outflow computation
 Q_zmin = 38,  # cm; qcoef_zmin
 Qomax = 0.0, # maximum discharge  hm3/day
 Qimax = 0, # maximum inflow  (hm3/day)
 Width = 1.55, # km
 Bypass_elev = 121.92, # z_byp; mean depth at which bypass begins (m) from input (cm)
 Seepout_Rate = 0.00789, # outflow seepage rate per unit head (m/day)/m from input cm/d/cm
 Seepout_Elev = 0.0, # elevation controling outflow seepage rate (input cm)
 Seepin_Rate  = 0.0,
 Seepin_Elev  = 0.0,
 ShutdownET = TRUE,
 force_Q_out = FALSE,
 wrap_interp = TRUE,
 Zinit = 40,  # cm; initial water column depth
 Qin_Frac = 0.22, # inflow_frac; fraction of basin flows going into this cell
 Zrelease = 0,   # cm; z_release; minimum depth for releases
 RecycleQ = 0,
 DutyCycle = 0.95, # duty [for P cycling]
 Cmax = 2000
)

pparams <- list(
 C1000 = 22,        # _c1000_1
 Cstar = 3,         # cstar_1
 Ks_per_yr = 16.8, # ks_1
 Z1 = 40,           # z_1
 Z2 = 100,          # z_2
 Z3 = 200,          # z_3
 Chalf = 300,       # _kc2
 K2Coef1 = 0,              # k2res
 Ytrans = 0,             # y00
 Ysigma = 0,             # ys00
 Czero = 0,                # CZero = 0
 C_rain = 10,              # rainfall P concentration (ug/L)
 DryDepo = 20,             # atmos (mg/m2-yr)
 SeasonalFactor = 0,       # cstar_2 ##OFF for base parity
 C1000_2 = NULL,           # _c1000_2
 ks_2 = 0,                 # ks_2
 zh_2 = 0,                 # zh_2
 k_depth_penalty = 1,      # m/yr
 seepage_c = 20,           # max seepage outflow concentration (ug/L)
 seepin_conc = 0,          # Inflow Seepage Conc (ug/L)
 C_init_ppb = 30,                # water column initial conc (ug/L)
 Y_init_mgm2 = 3387.67297548954, # biomass storage (mg/m2)
 n_tanks = 3,
 Nsteps = 4
)

params <- modifyList(params,pparams)

##  choose model structure
ttankS  <- params$n_tanks    # tanks in series (can be fractional)
Nsteps  <- params$Nsteps     # RK substeps per day

##  build tank geometry once
tanks <- dmsta_build_tanks(params$A_cell, ttankS)

##  build kinetics once: 3 modules STA/PSTA/RES
P_MODEL_BUILDERS <- list(
STA  = build_STA,
PSTA = build_PSTA,
RES  = build_RES
)
ppar <- build_P_kin_slots(
  mods     = c("STA", "PSTA", "RES"),
  registry = NULL,
  pparams  = params,
  Dpy      = 365.25,
  DutyCycle = params$DutyCycle
)
 validate_P_paramsK(ppar)

##  constants used by dmsta_deriv_mass
constants <- list(
  Cmax = params$Cmax,
  C_rain = params$C_rain,
  DryDepo = params$DryDepo / 365.25,   # mg/m2-day (VBA divides by Dpy)
  seepin_conc = params$seepin_conc,
  seepout_conc_max = params$seepage_c, # cap seepage concentration
  fseep_recycle = 0,                   # set later if you add seepage recycle fractions
  fseep_out = 0
)

##  initial conditions
Z_init_m <- cm_to_m(params$Zinit)
V_init   <- params$A_cell * Z_init_m

P_state0 <- dmsta_p_init_state(
  tanks,
  Z_init_m      = Z_init_m,
  C_init_ppb    = params$C_init_ppb,
  Y_init_mgm2   = params$Y_init_mgm2
)

out <- dmsta_flowP_series(
  series = series,
  params = params,
  ttankS = ttankS,
  Nsteps = Nsteps,
  tanks  = tanks,
  ppar   = ppar,
  constants = constants,
  V_init = V_init,
  init_P_state = P_state0,
  return_steps  = FALSE
 )
 out$results
 }

}
