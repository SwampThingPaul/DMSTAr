% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmsta_P_engine.R
\name{dmsta_flowP_series}
\alias{dmsta_flowP_series}
\title{Run a coupled hydrology–phosphorus simulation over a time series}
\usage{
dmsta_flowP_series(
  series,
  params,
  pparams = NULL,
  ttankS = 3,
  Nsteps = 4L,
  N_plant = 30L,
  Qmethod = c("RK4", "Euler", "RKF45", "custom"),
  Pmethod = c("RK4", "Euler"),
  integrator_fun = NULL,
  interp_option = 2L,
  ppar = NULL,
  constants = NULL,
  tanks = NULL,
  V_init = NULL,
  init_P_state = NULL,
  return_steps = FALSE,
  ...
)
}
\arguments{
\item{series}{Data frame containing daily forcing inputs. Must include
at least \code{Date}, \code{Qi}, \code{Ci}, \code{Rain}, \code{Et},
and \code{Zcontrol}. Optional columns include release and recycle terms.}

\item{params}{List of hydrologic and phosphorus parameters.}

\item{pparams}{Optional list of phosphorus parameters to merge into
\code{params}.}

\item{ttankS}{Numeric. Number of tanks in series (may be fractional).}

\item{Nsteps}{Integer. Number of hydrology sub-steps per day.}

\item{N_plant}{Integer. Window length (days) used to compute rolling
mean depth for \code{Z_plant}.}

\item{Qmethod}{Character string specifying the hydrology integrator.}

\item{Pmethod}{Character string specifying the phosphorus integrator.}

\item{integrator_fun}{Optional custom hydrology integrator function.}

\item{interp_option}{Integer. Control-depth interpolation option.}

\item{ppar}{Optional precomputed phosphorus kinetic parameter list.}

\item{constants}{Optional list of constants used by phosphorus derivatives.}

\item{tanks}{Optional pre-built tanks-in-series configuration.}

\item{V_init}{Optional initial volume (hm^3). If \code{NULL}, derived
from depth initialization rules.}

\item{init_P_state}{Optional initial phosphorus state. If \code{NULL},
initialized from depth and concentration parameters.}

\item{return_steps}{Logical. If \code{TRUE}, store daily hydrology
sub-step outputs in the result metadata.}

\item{...}{Additional arguments passed to daily hydrology integration.}
}
\value{
An object of class \code{"dmsta_result"} with elements:
\describe{
\item{results}{Data frame of daily hydrology and phosphorus outputs.}
\item{budgets}{List containing water and phosphorus mass budget
data frames.}
\item{meta}{Metadata describing model configuration, initialization,
and methods used.}
}
}
\description{
Simulates DMSTA hydrology and phosphorus dynamics over a multi-day
time series for a single cell. This function manages time-series
iteration, initialization, rolling diagnostics (e.g., \code{Z_plant}),
and aggregation of daily results, while delegating daily integration
to \code{dmsta_flowP_day()}.
}
\details{
Series-level operational semantics (e.g., HydroIndex-style presence
flags, optional release warm-up periods) are applied here and passed
downstream as daily inputs.
}
