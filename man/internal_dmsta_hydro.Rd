% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmsta_hydrology.R
\name{internal_dmsta_hydro}
\alias{internal_dmsta_hydro}
\alias{dmsta_DerivFlow}
\title{DMSTA hydrology engine (internal)}
\usage{
dmsta_DerivFlow(
  V,
  A_cell,
  Qi,
  Rain,
  Et,
  Zcontrol,
  Zcontrol1 = Zcontrol,
  Zcontrol2 = Zcontrol,
  Step,
  StepFrac,
  Nsteps,
  Ddt,
  params,
  Q_zmin = 0,
  Zweir = 0,
  Zmin = 0,
  Vmin = NULL,
  Seepin_Rate = 0,
  Seepin_Elev = 0,
  Seepout_Rate = 0,
  Seepout_Elev = 0,
  RecycleQ = 0,
  Qrelease = 0,
  Bypass_elev = 0,
  Qimax = 0,
  Qomax = 0,
  Q_a = 0,
  Q_b = 1,
  Width = 1,
  ShutdownET = TRUE,
  has_outflow_constraint = FALSE,
  has_depth_constraint = FALSE,
  Qr_0 = 0
)
}
\arguments{
\item{V}{Numeric scalar. Current storage volume (hm^3).}

\item{A_cell}{Numeric scalar. Cell area (km^2).}

\item{Qi}{Numeric scalar. Inflow rate (hm^3/day).}

\item{Rain}{Numeric scalar. Rainfall rate (m/day).}

\item{Et}{Numeric scalar. Evapotranspiration rate (m/day).}

\item{Zcontrol}{Numeric scalar. Control depth for the current day (m).}

\item{Zcontrol1}{Numeric scalar. Control depth for the previous day (m).
Defaults to \code{Zcontrol}.}

\item{Zcontrol2}{Numeric scalar. Control depth for the next day (m).
Defaults to \code{Zcontrol}.}

\item{Step}{Integer. Sub-step index within the day (1-based).}

\item{StepFrac}{Numeric scalar. Fractional position within the current
sub-step (0–1).}

\item{Nsteps}{Integer. Total number of sub-steps per day.}

\item{Ddt}{Numeric scalar. Sub-step duration (days).}

\item{params}{List of hydrologic parameters. Must include
\code{interp_option} specifying control-depth interpolation mode.}

\item{Q_zmin}{Numeric scalar. Minimum depth offset required for outflow (m).}

\item{Zweir}{Numeric scalar. Weir crest elevation (m).}

\item{Zmin}{Numeric scalar. Minimum allowable depth (m).}

\item{Vmin}{Numeric scalar. Minimum allowable volume (hm^3).
If \code{NULL}, defaults to \code{Zmin * A_cell}.}

\item{Seepin_Rate}{Numeric scalar. Seep-in rate coefficient.}

\item{Seepin_Elev}{Numeric scalar. External groundwater elevation for seep-in (m).}

\item{Seepout_Rate}{Numeric scalar. Seep-out rate coefficient.}

\item{Seepout_Elev}{Numeric scalar. External groundwater elevation for seep-out (m).}

\item{RecycleQ}{Numeric scalar. Internal recycle inflow (hm^3/day).}

\item{Qrelease}{Numeric scalar. Explicit release flow (hm^3/day).}

\item{Bypass_elev}{Numeric scalar. Elevation above which full bypass occurs (m).}

\item{Qimax}{Numeric scalar. Maximum allowable inflow before bypass (hm^3/day).}

\item{Qomax}{Numeric scalar. Maximum allowable outflow (hm^3/day).
Negative values indicate a minimum bypass constraint.}

\item{Q_a}{Numeric scalar. Hydraulic discharge coefficient.}

\item{Q_b}{Numeric scalar. Hydraulic discharge exponent.}

\item{Width}{Numeric scalar. Effective weir width (km).}

\item{ShutdownET}{Logical. If \code{TRUE}, evapotranspiration is reduced
to prevent storage from falling below \code{Vmin}.}

\item{has_outflow_constraint}{Logical. Indicates presence of an explicit
outflow constraint (DMSTA HydroIndex(2) > 0).}

\item{has_depth_constraint}{Logical. Indicates presence of a depth
constraint (DMSTA HydroIndex(1) > 0).}

\item{Qr_0}{Numeric scalar. Fixed outflow rate used when
\code{has_outflow_constraint = TRUE} and
\code{has_depth_constraint = FALSE}.}
}
\value{
A named list containing instantaneous derivatives and
diagnostic quantities:
\describe{
\item{Dvdt}{Rate of change of storage volume (hm^3/day).}
\item{Qot}{Total outflow including releases (hm^3/day).}
\item{Qo}{Overflow (depth-dependent) outflow only (hm^3/day).}
\item{Qnet}{Net inflow excluding overflow (hm^3/day).}
\item{Vnext}{Trial volume at end of sub-step before trimming (hm^3).}
\item{Z}{Current depth (m).}
\item{Zcont}{Interpolated and constrained control depth (m).}
\item{Vcontrol}{Control volume corresponding to \code{Zcont} (hm^3).}
\item{Etest}{Effective evapotranspiration rate after any shutdown (m/day).}
\item{AtmoS}{Net atmospheric flux (rain – ET) as volume (hm^3/day).}
\item{Seepin}{Seep-in flux (hm^3/day).}
\item{Seepout}{Seep-out flux (hm^3/day).}
\item{Bypass}{Bypass flow (hm^3/day).}
\item{Delta}{Fractional position within the day used for interpolation.}
}
}
\description{
Internal helpers implementing DMSTA hydrologic integration, including
within-day control depth interpolation, derivative evaluation, and RK4 stepping.

Computes instantaneous hydrologic fluxes and the rate of change of
storage volume for a single within-day sub-step, following the
DMSTA \code{DerivFlow} logic implemented in VBA (Module1).
}
\details{
This function performs \strong{no time integration}. It evaluates the
hydrologic balance at a specific fractional position within the day
(\code{StepFrac}) and returns the instantaneous derivative and
associated flux diagnostics. Numerical integration over a day is
handled by higher-level integrators (e.g., RK4, Euler).

Control-depth interpolation, outflow constraints, bypass logic,
seepage, evapotranspiration shutdown, and release semantics follow
DMSTA conventions and are governed by the supplied parameters and
HydroIndex-style flags.

This function corresponds closely to the DMSTA VBA routine
\code{DerivFlow} and is intended for internal use by hydrologic
integrators. It should not be called directly by users.

For strict DMSTA parity, control-depth interpolation behavior is governed
by \code{params$interp_option}, with \code{2} (mid-day values) as the
default.
}
\keyword{internal}
