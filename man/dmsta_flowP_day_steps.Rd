% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmsta_P_engine.R
\name{dmsta_flowP_day_steps}
\alias{dmsta_flowP_day_steps}
\title{Integrate hydrology–phosphorus dynamics over one day using sub-day steps}
\usage{
dmsta_flowP_day_steps(
  V,
  P_state,
  tanks,
  inputs,
  params,
  ppar,
  constants,
  Qmethod = c("RK4", "Euler", "RKF45", "custom"),
  Pmethod = c("RK4", "Euler"),
  Nsteps = 4L,
  Z_plant = 0,
  integrator_fun = NULL,
  interp_option = 2L,
  ...
)
}
\arguments{
\item{V}{Numeric scalar. Volume at the start of the day (hm^3).}

\item{P_state}{List with elements \code{M} and \code{S}, numeric vectors
of length \code{tanks$Ntanks} giving initial phosphorus state (kg)
in each tank.}

\item{tanks}{List describing the tanks-in-series configuration
(e.g., output of \code{dmsta_build_tanks()}).}

\item{inputs}{List of daily forcing inputs (Qi, Ci, Rain, Et, Zcontrol,
releases, recycle flows, etc.), already prepared for the current day.}

\item{params}{List of hydrologic parameters for the cell.}

\item{ppar}{List of phosphorus kinetic parameters (output of
\code{build_P_kin_slots()}).}

\item{constants}{List of physical and chemical constants used by
\code{dmsta_DerivMass()}.}

\item{Qmethod}{Character string specifying the hydrology integrator
(e.g., \code{"RK4"}, \code{"Euler"}, \code{"RKF45"}, \code{"custom"}).}

\item{Pmethod}{Character string specifying the phosphorus integrator
(currently \code{"RK4"} or \code{"Euler"}).}

\item{Nsteps}{Integer. Number of sub-day hydrology steps per day.}

\item{Z_plant}{Numeric scalar. Rolling-mean depth (m) used for reservoir
penalty blending in phosphorus kinetics.}

\item{integrator_fun}{Optional custom hydrology integrator function,
used when \code{Qmethod = "custom"}.}

\item{interp_option}{Integer. Control-depth interpolation option
(DMSTA semantics; default is mid-day).}

\item{...}{Additional arguments passed to hydrology integrators.}
}
\value{
A list with elements:
\describe{
\item{hyd}{Normalized hydrology object returned by
\code{dmsta_flow_day_steps()}.}
\item{P_state_end}{List with updated \code{M} and \code{S} vectors
at end of day.}
\item{accum}{Named list of daily accumulated flows, loads, and
phosphorus mechanism totals (uptake, recycle, sedimentation, burial).}
\item{storage}{List of starting and ending total phosphorus storage
for the day.}
}
}
\description{
Performs coupled hydrology and phosphorus integration over a single day
for a storage cell by looping over hydrologic sub-steps and integrating
phosphorus dynamics within each sub-step. Hydrology stepping is provided
by \code{dmsta_flow_day_steps()}, and phosphorus integration is delegated
to \code{dmsta_P_step()}.
}
\details{
This function assumes a storage cell with one or more tanks in series.
Node behavior (cells without storage or P state) is handled upstream
in \code{dmsta_flowP_day()} and should not be routed through this function.
}
\keyword{internal}
