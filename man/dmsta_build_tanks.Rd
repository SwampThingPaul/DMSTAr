% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/P_tanks.R
\name{dmsta_build_tanks}
\alias{dmsta_build_tanks}
\title{Build DMSTA tank partitioning for a cell}
\usage{
dmsta_build_tanks(A_cell, ttankS, snap_last = TRUE)
}
\arguments{
\item{A_cell}{Numeric scalar > 0. Cell area (units consistent with the rest
of the DMSTA implementation).}

\item{ttankS}{Numeric scalar > 0. Effective number of tanks. May be fractional.}

\item{snap_last}{Logical; if \code{TRUE}, force the last cumulative fraction
to exactly \code{1.0}. Default is \code{TRUE}.}
}
\value{
A named \code{list} with elements:
\describe{
\item{Ntanks}{Integer number of tanks.}
\item{A_Tank}{Numeric vector of tank areas, length \code{Ntanks}.}
\item{F_Tank}{Numeric vector of tank area fractions (\code{A_Tank / A_cell}).}
\item{Fcum}{Numeric vector of cumulative area fractions (\code{cumsum(F_Tank)}).}
}
}
\description{
Internal helper that partitions a single cell area into a series of
conceptual "tanks" used by DMSTA. The number of tanks is derived from
\code{ttankS}; if \code{ttankS} is fractional, the final tank receives
the fractional area share and all preceding tanks receive equal shares.
}
\details{
The function also returns per-tank area fractions and cumulative fractions
(useful for mapping depth/area relationships). Optionally, the last cumulative
fraction can be "snapped" to exactly 1.0 for numerical stability.
}
\seealso{
\code{dmsta_p_init_state()} for initializing state vectors
compatible with the returned \code{Ntanks}.
}
