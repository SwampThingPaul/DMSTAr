% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Network_cases.R
\name{dmsta_case_network}
\alias{dmsta_case_network}
\alias{build_routes_from_net_table}
\alias{extract_df}
\alias{stream_map_cols}
\alias{topo_order_cases}
\alias{lag_vec}
\alias{run_network_of_cases}
\title{Case-level network simulation utilities for DMSTAr}
\usage{
build_routes_from_net_table(
  net_table,
  outlet_count = 5L,
  case_col = "CaseName"
)

extract_df(res, level = c("case", "cell"), cell_index = 1L)

stream_map_cols(stream, outflow_def = c("treated", "total"))

topo_order_cases(routes, case_names)

lag_vec(x, lag_days)

run_network_of_cases(
  cases,
  net_table = NULL,
  routes = NULL,
  outlet_count = 5L,
  verbose = TRUE,
  check_route = FALSE,
  ...
)
}
\arguments{
\item{net_table}{Optional DMSTA-style network table. If provided and \code{routes} is \code{NULL},
routing is built with \code{\link[=build_routes_from_net_table]{build_routes_from_net_table()}}.}

\item{outlet_count}{Integer number of outlet bins (used when parsing numeric destinations).}

\item{case_col}{Character name of the column in \code{net_table} containing
DMSTA CASE identifiers.}

\item{res}{A list-like object, typically returned by \code{dmsta_flowP_case()}.}

\item{level}{Character, one of \code{"case"} or \code{"cell"}.}

\item{cell_index}{Integer index of the cell to extract when \code{level = "cell"}.}

\item{stream}{Character stream name. Supported values:
\code{"bypass"}, \code{"release1"}, \code{"release2"}, \code{"outflow"}, \code{"seepage"}.}

\item{outflow_def}{Character definition for \code{"outflow"} mapping:
\code{"treated"} maps to treated outflow columns; \code{"total"} maps to total outflow columns.}

\item{routes}{Optional normalized routing table. If provided, \code{net_table} is ignored.}

\item{case_names}{Character vector of all CASE IDs participating in the network.}

\item{x}{Numeric vector.}

\item{lag_days}{Integer number of days to shift. Positive shifts forward in time;
negative shifts backward. Zero returns \code{x} unchanged.}

\item{cases}{Named list of CASE definitions. Names must be DMSTA CASE IDs.
Each element must contain at least:
\itemize{
\item \code{series_base}: a \code{data.frame} with columns \code{Date}, \code{Qi}, \code{Ci},
\code{Rain}, \code{Et}, \code{Zcontrol}
\item \code{cells}: a list of cell definitions for \code{dmsta_flowP_case()}
}}

\item{verbose}{Logical; if \code{TRUE}, prints execution order and per-CASE summaries.}

\item{check_route}{Logical; if \code{TRUE}, prints per-CASE stream discharge sums for debugging.}

\item{...}{Additional arguments passed through to \code{dmsta_flowP_case()}.}
}
\value{
A \code{data.frame} with one row per configured route, containing:
\describe{
\item{from_case}{Upstream CASE ID (character).}
\item{stream}{One of \code{"bypass"}, \code{"release1"}, \code{"release2"},
\code{"outflow"}, \code{"seepage"}.}
\item{to_type}{\code{"CASE"} or \code{"OUTLET"}.}
\item{to_id}{Downstream CASE ID (character) or outlet index (integer stored as character).}
\item{frac}{Routing fraction (numeric), currently always 1.}
\item{lag_days}{Integer travel-time lag in days, currently always 0.}
}

A \code{data.frame} containing the requested daily time series.

A named character vector of length 2 with names \code{Q} and \code{L},
giving the discharge and load column names.

Character vector of CASE IDs ordered such that upstream cases appear
before any downstream cases they route to.

Numeric vector of the same length as \code{x}.

A named list with elements:
\describe{
\item{order}{Character vector of CASE execution order.}
\item{case_results}{Named list of raw \code{dmsta_flowP_case()} results for each CASE.}
\item{routed_in}{Named list of routed inflow time series per CASE (\code{Q}, \code{L}).}
\item{outlets}{List of outlet-bin time series (\code{Q}, \code{L}) of length \code{outlet_count}.}
\item{outlet_summary}{\code{data.frame} summarizing total outlet discharge/load and FWC.}
\item{ledger}{\code{data.frame} of per-route totals (\code{total_Q}, \code{total_L}) for auditing.}
}
}
\description{
A collection of helper functions that support \strong{DMSTA-style CASE networks}
in \pkg{DMSTAr}. These utilities provide:

Converts a DMSTA workbook "Networks" table into a normalized routing
\code{data.frame} suitable for downstream case-level network simulation.
Each non-blank destination in the network table becomes one route row.

Flexible extractor for pulling either the case-level output (\code{level = "case"})
or a specific cell-level output (\code{level = "cell"}) from a list returned by
\code{dmsta_flowP_case()}.

Resolves a stream identifier (e.g., \code{"bypass"}, \code{"release1"},
\code{"outflow"}) to the corresponding discharge (\verb{Q_*}) and load
(\verb{L_*}) columns in a DMSTAr case output table.

Given a routing table and a set of case names, computes a topological order
(upstream to downstream) under the assumption that the case network is a DAG
(i.e., contains no directed cycles).

Applies a simple discrete lag/lead to a numeric vector, padding with zeros.
Positive \code{lag_days} shifts values later in time (prepends zeros);
negative values shift earlier in time (appends zeros).

Executes a set of CASE simulations in topological (upstream-to-downstream)
order and routes configured outflows/loads from upstream CASE outputs into
downstream CASE inflow forcing, emulating DMSTA workbook "Networks" behavior.
}
\details{
\itemize{
\item Parsing of DMSTA workbook "Networks" tables into routing definitions
\item Topological ordering of CASEs for downstream execution
\item Robust extraction of case- and cell-level output tables
\item Stream-to-column mapping for routing discharge and load
\item Vector lagging utilities for travel-time effects
\item A high-level wrapper to run downstream CASE networks
}

Together, these functions allow multiple DMSTA CASE simulations to be linked
into a strictly downstream network, emulating the behavior of the DMSTA VBA
workbook network framework while preserving DMSTAr's case- and cell-level APIs.

The network execution model follows standard DMSTA semantics:

\enumerate{
\item CASEs are executed in upstream-to-downstream order.
\item Routed discharge and load from upstream CASE outputs are \strong{added}
to downstream CASE inflows.
\item Concentrations are recomputed safely as \eqn{C = L / Q}.
\item Routes may terminate at downstream CASEs or numeric outlet bins.
}

These helpers are primarily used by \code{\link[=run_network_of_cases]{run_network_of_cases()}},
but many are also useful independently for diagnostics, testing, and
custom orchestration.

Destinations are interpreted as:
\itemize{
\item A positive integer: route to an \code{"OUTLET"} bin.
\item Otherwise: route to another \code{"CASE"} identified by string ID.
\item Blank / whitespace: ignored (no route for that stream).
}

The returned routes include \code{frac} (default 1) and \code{lag_days}
(default 0) to support future extensions such as flow splitting or travel-time.

Current DMSTAr documentation shows \code{dmsta_flowP_case()} returning a list
with \code{out$results$case} and \code{out$results$cells[[i]]} (or similarly named
cell outputs).
This function supports that nested style and also checks common legacy fields
such as \code{res$case} / \code{res$case_out}.

DMSTAr case outputs include standardized \verb{Q_*} and \verb{L_*} fields for
bypass, releases, seepage discharge, and outflows.

This uses Kahn's algorithm over CASE-to-CASE edges (routes with \code{to_type == "CASE"}).
If not all nodes can be ordered, the function stops, indicating a directed cycle.

For each CASE:
\enumerate{
\item Base inflow discharge and concentration (\code{Qi}, \code{Ci}) are sanitized.
\item Routed inflow discharge/load from upstream CASEs are added.
\item A safe routed inflow concentration is computed (\code{Cin = Lin / Qin}).
\item \code{dmsta_flowP_case()} is executed for that CASE.
\item Specified stream outputs are routed to downstream CASEs or outlet bins.
}

This wrapper relies on the standardized case output columns produced by
\code{dmsta_flowP_case()} (e.g., \code{Q_out_bypass}, \code{Q_out_treated},
\code{Q_out_total}, and corresponding \verb{L_*} fields).
It also expects that all CASEs share the same \code{Date} sequence (same length and alignment).

The outlet summary uses \code{fw()} to compute flow-weighted concentration (FWC) from total
load and discharge; ensure \code{fw()} is available in your namespace.
}
\section{Main user-facing functions}{

\itemize{
\item \code{\link[=build_routes_from_net_table]{build_routes_from_net_table()}}
\item \code{\link[=extract_df]{extract_df()}}
\item \code{\link[=run_network_of_cases]{run_network_of_cases()}}
}
}

\section{Internal helpers}{

\itemize{
\item \code{extract_case_df}
\item \code{stream_map_cols}
\item \code{topo_order_cases}
\item \code{lag_vec}
}
}

\examples{

net <- data.frame(
  CaseName    = c("STA1_DW", "STA1W"),
  Bypass_to   = c("", ""),
  Release1_to = c("", ""),
  Release2_to = c("", ""),
  Outflow_to  = c("STA1W", "1"),
  Seepage_to  = c("", ""),
  stringsAsFactors = FALSE
)
build_routes_from_net_table(net, outlet_count = 1L)

\dontrun{
out <- dmsta_flowP_case(series, cells, return_cell_series = TRUE)
df_case <- extract_df(out, level = "case")
df_cell1 <- extract_df(out, level = "cell", cell_index = 1)
}
\dontrun{
lag_vec(1:5, 2)   # 0 0 1 2 3
lag_vec(1:5, -2)  # 3 4 5 0 0
}
\dontrun{
# 1) Build cases
cases <- list(
 STA1_DW = list(
  series_base = STA1_DW_input,   # data.frame with Date, Qi, Ci, Rain, Et, Zcontrol
  cells       = STA1_DW_cell     # list of dmsta_make_cell(...) objects
),
STA1W = list(
  series_base = STA1W_input,
  cells       = STA1W_cell
)
)

# 2) Build/parse routes
net <- data.frame(
  CaseName    = c("STA1_DW", "STA1W"),
  Bypass_to   = c("", ""),
  Release1_to = c("", ""),
  Release2_to = c("", ""),
  Outflow_to  = c("STA1W", "1"),
  Seepage_to  = c("", ""),
  stringsAsFactors = FALSE
)
routes <- build_routes_from_net_table(net_table, outlet_count = 1L)

# 3) Run network
out <- run_network_of_cases(
  cases = cases,
  routes = routes,
  Nsteps = 4L,
  return_cell_series = TRUE
)

out$outlet_summary
head(out$ledger)
}
}
\seealso{
\code{\link[=dmsta_flowP_case]{dmsta_flowP_case()}}
}
\keyword{internal}
