% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dmsta_P_engine.R
\name{dmsta_flowP_day}
\alias{dmsta_flowP_day}
\title{Simulate one day of coupled hydrology and phosphorus dynamics}
\usage{
dmsta_flowP_day(
  V,
  P_state,
  tanks,
  inputs,
  params,
  ppar,
  constants,
  Qmethod = c("RK4", "Euler", "RKF45", "custom"),
  Pmethod = c("RK4", "Euler"),
  Nsteps = 4L,
  Z_plant = 0,
  integrator_fun = NULL,
  interp_option = 2L,
  ...
)
}
\arguments{
\item{V}{Numeric scalar. Volume at the start of the day (hm^3).}

\item{P_state}{List with elements \code{M} and \code{S}, giving initial
phosphorus state (kg). For node cells, this is ignored.}

\item{tanks}{List describing the tanks-in-series configuration.}

\item{inputs}{List of daily forcing inputs (Qi, Ci, Rain, Et, Zcontrol,
releases, recycle flows, etc.).}

\item{params}{List of hydrologic parameters for the cell.}

\item{ppar}{List of phosphorus kinetic parameters.}

\item{constants}{List of physical and chemical constants used by
phosphorus derivative calculations.}

\item{Qmethod}{Character string specifying the hydrology integrator
(\code{"RK4"}, \code{"Euler"}, \code{"RKF45"}, or \code{"custom"}).}

\item{Pmethod}{Character string specifying the phosphorus integrator
(\code{"RK4"} or \code{"Euler"}).}

\item{Nsteps}{Integer. Number of hydrology sub-steps per day.}

\item{Z_plant}{Numeric scalar. Rolling-mean depth (m) used for reservoir
penalty blending.}

\item{integrator_fun}{Optional custom hydrology integrator function.}

\item{interp_option}{Integer. Control-depth interpolation option
(DMSTA semantics).}

\item{...}{Additional arguments passed to hydrology integrators.}
}
\value{
A list with elements:
\describe{
\item{results}{List of daily end-of-day states and aggregated
hydrology and phosphorus outputs.}
\item{budgets}{List containing daily water and phosphorus mass budgets.}
\item{meta}{Metadata describing the simulation day, methods used,
and configuration flags.}
}
}
\description{
Simulates a single day of DMSTA hydrology and phosphorus behavior for
either a storage cell or a node. Node behavior (no storage, no phosphorus
state) is handled algebraically, while storage cells are integrated using
sub-day hydrology steps coupled to phosphorus integration.
}
\details{
This function acts as the top-level daily orchestrator for coupled
hydrology–phosphorus dynamics. Numerical integration details are
delegated to lower-level step functions.

For strict DMSTA parity, use \code{Qmethod = "RK4"} and
\code{Pmethod = "RK4"} with no operational overrides. Optional operational
features (e.g., release pauses) are applied upstream in series-level
drivers and are not part of the DMSTA core formulation.
}
\keyword{internal}
